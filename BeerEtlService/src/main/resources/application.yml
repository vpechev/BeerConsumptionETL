# Configuration for the acquired port by the service
server:
  port: 8082

# Logging level configuration
logging:
  level:
    root: info

# Health check endpoint configuration
endpoints:
  health:
    enabled: true
    web:
      path: health

#management:
#  security:
#    enabled: false
#  health:
#    binders:
#      enabled: false
#    rabbit:
#      enabled: false
#  info:
#    git:
#      mode: full

# Spring configuration


#spring.jpa.show-sql=true
#spring.jpa.hibernate.dialect=org.hibernate.dialect.SQLServer2012Dialect
#spring.jpa.hibernate.ddl-auto = create-drop

spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433
    databaseName: beer
    username: sa
    password: My_pa77min8$
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDrivera
  data:
    mongodb:
      host: localhost
      port: 27017
      database: beer
  application:
    name: beer-etl
  cloud:
    stream:
      bindings:
        output:
          contentType: application/json
          destination: topic.beer.sql.data
          binder: rabbit
        input:
          destination: queue.beer
          group: data
          binder: rabbit
      binders:
        rabbit:
          type: rabbit
          environment:
            spring:
              rabbitmq:
                host: localhost
                port: 5671
                username: admin
                password: admin
                virtual-host: dev
                ssl:
                  enabled: false
      rabbit:
        bindings:
          output:
            producer:
              declareExchange: false
              routing-key-expression: "'key.beer.data'"